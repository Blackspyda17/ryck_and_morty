query AllCharacters($page : Int) {
    characters(page: $page) {
        info {
            next
        }
        results {
            id
            name
            species
            type
            status
            gender
            image
        }
    }
}

